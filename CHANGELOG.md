## v3.6.1

- LLM 工具的开始提示改为真正使用当前会话的 AstrBot 人格：插件读取会话实际选中的 Persona，并通过当前 Chat Provider 生成一句简短承接话；人设自拍会以角色本人身份回应，不再使用“嗯嗯，稍等，我弄一下”等插件固定模板。
- 人格化开始提示只会在参考图与额度检查通过后请求；失败或超时时会静默继续图片任务，不再回退到插件预设口吻。配置说明明确提示开启后每次实际任务会增加一次小型模型请求，关闭后仅省略开始提示，不影响完成后的正常人格回复。
- 工具失败和普通成功收尾移除固定参考台词，统一要求沿用当前 AstrBot 人格、称呼习惯和上下文关系。

## v3.6.0

- `#bnn` 的多人艾特头像隐私保护升级为最终硬性指令：提示词只读取用户实际发送的纯文本，清除适配器展开的昵称、群名和账号文本；在翻译/优化与自定义反向提示词之后，最后追加“图片不带艾特名称、QQ 号或 ID”的中英双语约束，并原样传给所有回退渠道。用户已写同类要求时不会重复追加。
- 新增持久化任务中心和图像工作台：可查看运行中、成功、失败和取消任务，保留请求输入，支持重新执行；文生图、图生图和收藏记录继续保存实际成功渠道、模型、输入原图、提示词与用户/群名称快照。
- 新增人设每日稳定状态：可选择真人、动漫或自动形象类型，并按日期稳定组合穿搭、心情和早晨/白天/傍晚/夜间状态；“发自拍”等成功后的文字仍由当前 AstrBot 人格自然回复。
- Dashboard 增加渠道模型刷新、配置脱敏导入导出及导入失败完整回滚；错误信息进一步隐藏常见 Key 和 URL 查询密钥，避免管理与排障日志泄漏秘密。
- 完整复核主上游 `8fe27a4`：61 个上游配置键均保留，公开命令和旧别名继续兼容，44 个上游默认预设内容完整一致；另增加捧脸、变真人、果冻化、变COS、漫画封面、证件照、男友视角和漏腰 8 个可编辑增强预设，共 52 个。

## v3.5.2

- `#bnn` 现在明确支持自动获取被艾特用户的 QQ 头像作为参考图；可一次艾特多人，头像按艾特顺序提交，重复艾特自动去重，并忽略机器人自身。旧的 `#头像图生图` 继续只使用发送者本人头像。
- 新增参考图回显保护：任意带参考图请求若收到与某张输入图相同的结果，会将当前渠道判定为失败且不发送、不计为成功记录，并继续执行多渠道回退。
- 奢侈模式会跳过先返回的参考图回显，继续等待同批并发请求中的真实结果；同图检测兼容原字节、无损换编码和极近似重编码。

## v3.5.1

- 重排 Dashboard 概览：当前调度收紧为统计条下方的紧凑状态栏，不再占据左侧大面板；渠道芯片显示启用状态、主/备用角色和模型，过多渠道可横向查看。
- 概览主体改为单列纵向布局，今日用量和最近成功图片使用完整可用宽度，减少并列面板造成的空白；最近成功区域展示最多 4 张带渠道、任务类型和会话身份的缩略图。
- 移动端和黑客终端主题补齐调度状态栏、渠道芯片与最近成功卡片的响应式和直角视觉适配。

## v3.5.0

- 文生图、图生图和收藏记录新增群聊与个人筛选。群聊下拉可选择具体群、仅私聊或全部会话，个人下拉可选择具体 QQ 用户；两个条件可叠加，筛选统计和分页会同步更新，图片仍按进入视口后懒加载。
- Dashboard 侧边栏重新设计为分组导航和紧凑控制条，主题选择改为向上弹出的四主题菜单，连接状态、重载与保存不再占用大块卡片。保存按钮改为半透明青紫琥珀荧光渐变。
- 新增独立的“黑客终端”主题：使用终端字体、直角框架、命令行式导航、硬边状态反馈、绿色/青色/琥珀多色状态系统、扫描线与电路网络背景，并持续保存主题选择。

## v3.4.3

- Dashboard 的文生图、图生图和收藏成功记录现在会显示该图片实际成功使用的绘图渠道（渠道 ID 与显示名称）及实际模型；渠道回退成功时会记录最终成功的渠道，而不是最初尝试的渠道。升级前缺少这项历史数据的记录会明确标示“渠道未记录”。

## v3.4.2

- 概览页同步采用紧凑仪表盘布局：渠道、今日生成、预设和参考图统计收为一条；当前调度与今日用量改为并列面板，首屏优先展示实际状态和表格内容。

## v3.4.1

- Dashboard 的文生图、图生图和收藏记录页改为紧凑统计工具栏，移除占据首屏的大统计卡与说明面板，让图片记录直接紧随筛选与操作按钮显示。
- 修复旧版图生图记录展开后重复显示“未缓存输入原图”的问题：卡片仅标识图生图类型，展开后只保留一条清晰的不可恢复说明。

## v3.4.0

- 图生图成功记录现在会保存本次请求实际使用的所有输入原图，包括带图或引用图的 `#画` / `#bnn`、QQ 头像图生图、人设拍照、预设参考图、批量图生图与 PDF 批量路径。每张输入图均以独立、受鉴权的缓存文件保存，不暴露本地目录。
- Dashboard 增加独立的“图生图工作台”入口，并为成功记录提供“文生图 / 图生图 / 全部”筛选。图生图卡片可按需展开查看输入原图缩略预览、结果图、提示词和输入原图下载入口；列表本身仍保持分页和懒加载，避免输入图较多时拖慢页面。
- 收藏或锁定记录现在同时保护结果图、输入原图和两类缩略图；过期清理、手动删除和孤儿文件清理也会同步处理这些素材。缓存用量统计包含输入原图，以便准确评估磁盘占用。
- 旧版图生图记录如未缓存输入原图，会明确显示“无法可靠恢复”，绝不将当前图片冒充历史原图。若记录来自人设或预设，Dashboard 可额外展示当前人设/预设参考图作为候选，并始终标注“非本次历史原图”。

## v3.3.4
- 修复 AstrBot 原生配置页保存“当前主绘图渠道”或“带参考图优先渠道”后，热重载时被旧 `dynamic_config.json` 反向覆盖的问题。动态兜底会记录保存失败前的原生值；只有原生值后来确实变更时才让原生配置胜出，不再把 AstrBot 补齐默认项的自动写入误判为人工保存。将主渠道留空会可靠恢复“自动”，将参考图渠道留空会可靠恢复“跟随主渠道”。

## v3.3.3
- 重组插件配置的展示顺序：所有原有键名保持不变，并以 `【命令与帮助】`、`【绘图渠道与回退】`、`【访问权限】`、`【额度与签到】`、`【提示词处理】`、`【人设与参考图】` 等模块标签连续排列，减少长配置列表中的误配；每项原有提示说明仍完整保留。
- 修复 Dashboard 成功记录缩略图全部显示“预览不可用”：缩略图改为经 AstrBot Plugin Page 鉴权桥接按需返回，不再由嵌入页直接请求受保护的二进制地址。
- 继续优化成功记录与收藏性能：列表不再预先传输每条完整提示词，展开单条记录才读取；缩略图进入视口后以最多两路并发加载并复用会话缓存；收藏筛选在服务端复用变更前的索引视图。
- 深色、浅色和爱丽丝主题均恢复自适应细网格网络背景，使用各自低对比配色并保持半透明面板下的文字可读性。

## v3.3.2
- 修复 QQ 图片发送“回执超时”的误报：仅当异常同时包含 QQ `sendMsg`/`NodeIKernelMsgService` 标识与超时时，才视为图片可能已送达，不重发图片、不把任务错误返回给 LLM；其他发送错误仍会正常报告。避免图片实际已发出后又收到失败提示或重复图片。
- 优化 Dashboard 成功记录与收藏：列表接口不再逐张读取原图、压缩并内联 Base64。缩略图只在卡片进入可见区域时通过受鉴权接口获取，并持久缓存为小型 JPEG；切换历史/收藏和翻页会短暂复用已加载列表，手动刷新、收藏、锁定、删除和清理后会立即失效。
- 缩窄 Dashboard 桌面侧栏并移除导航项前的 `//` 装饰；保留原有玻璃霓虹、主题和激活状态表现。
- 自动清理和手动清理会一并删除过期成功记录对应的缩略图；收藏或锁定的记录及其原图、提示词和缩略图仍会保留。

## v3.3.1
- 修复 Dashboard 上传的人设/预设参考图与聊天消息图片走不同格式路径的问题：绘图请求前会将已保存的 WebP、GIF、BMP、TIFF 等参考图标准化为 PNG，和聊天取图保持一致；原始参考图文件及 Dashboard 预览不会被改写。
- 修正文档中的排障判断：`#手办化` 本身就是图生图，能成功说明至少有一条带参考图调用链可用；人设拍照仍失败时，应检查已保存人设图的格式、实际路由渠道/模型及上游响应。

## v3.3.0
- Dashboard 侧栏重构为独立的玻璃霓虹控制台：品牌状态、三套分段皮肤、重载、保存和提示信息具有明确层级，不再是普通下拉框与按钮的轻量换色；皮肤仍会独立持久化。
- 新增带参考图专用路由。人设拍照、QQ 头像图生图、预设参考图与普通图生图可选择独立优先渠道和专用模型；渠道可标记为仅文生图，带参考图请求会自动跳过它并继续尝试合格回退渠道。
- 公开排障说明明确区分文生图与 `/v1/images/edits`：编辑端点 503 是中转站或其上游故障，不是 AstrBot LLM 工具注册问题，也不是“多上传几张人设图”造成的。

## v3.2.9
- Dashboard 三套皮肤统一为更克制的玻璃霓虹视觉：默认深色、浅色与爱丽丝主题都使用半透明面板、细边框和受控的青紫/琥珀状态色；侧栏底部的皮肤选择、刷新与保存区域同步改为半透明霓虹控件。
- 新增 `dashboard_theme` 配置和独立主题保存接口。选择皮肤会立即写入插件配置及动态备份，重新打开 Dashboard、更换浏览器或本地存储不可用时，仍以服务端保存的主题为准；切换主题不会提交尚未保存的其他表单修改。

## v3.2.8
- Dashboard 新增“爱丽丝”紫粉霓虹皮肤：保留深色和浅色皮肤，侧栏底部可直接选择，选择结果会保存在当前浏览器中。
- 侧栏品牌标记改为随插件发布的爱丽丝像素头像资源，不依赖外链；静态资源通过 AstrBot Plugin Page 的相对路径加载。

## v3.2.7
- 修复 Dashboard 人设参考图的单张删除与“清空人设图”无响应问题：AstrBot 嵌入式页面不再依赖可能被拦截的浏览器原生确认框，改用页面内确认窗口后才执行删除请求。
- 统一成功记录删除、过期缓存清理和额度重置的确认交互；支持取消、Esc 和点击遮罩安全退出，避免嵌入页面中的高风险操作出现无反馈。

## v3.2.6
- 用量表、额度表和成功记录现在同时展示昵称/群名称与 QQ 号/群号；QQ 号和群号仍是唯一的统计、额度和权限键，昵称或群名变化不会拆分数据。
- 新增 `identity_labels.json` 保存最近一次见到的显示名称；新成功记录保存当时的名称快照，旧记录在没有快照时会尽量显示最新已知名称。
- Dashboard 成功记录新增原图下载按钮，下载仍经过受鉴权的插件接口，不暴露本地缓存路径。
- Dashboard 侧栏新增“收藏”快捷入口，并可在成功记录页一键切换为仅查看收藏。

## v3.2.5
- 新增成功记录库：每个成功结果都会缓存最终发送的原图，并记录请求提示词、用户、群、时间、模型、预设、任务类型、尺寸和文件大小。
- Dashboard 新增“成功记录”页，支持受鉴权缩略图、分页、缓存统计、提示词查看、收藏、锁定、手动删除和过期清理。
- 新增 `generation_cache_retention_days` 配置，默认保留 7 天；插件启动时及每 24 小时自动清理过期的未收藏、未锁定记录。收藏或锁定的图片和提示词不会被自动清理。
- 统一普通、批量、PDF、头像图生图、人设拍照等所有成功路径的用量统计与历史记录，避免部分入口漏记。

## v3.2.4
- 修复 Dashboard “今日用量”错误显示为用户额度表的问题；现在会按用户和群分别展示当天成功图片数。
- 跨天且尚未有新请求时不再把前一天的统计误标为“今日用量”。

## v3.2.3
- 新增自定义绘图反向提示词：Dashboard 与插件配置均可维护，覆盖 `#bnn` 和可配置的自定义绘图指令（默认 `#画`）的文生图、图生图。
- 采用跨渠道兼容的 `Negative prompt:` 文本段，并在提示词翻译/优化之后追加，确保主渠道和回退渠道使用一致限制；预设和其他入口不受影响。

## v3.2.2
- 修复 New API 的 `gpt-image-*` 人设拍照/图生图在全局选择 2K 或 4K 时仍向 `/v1/images/edits` 发送不兼容尺寸的问题。带参考图请求会自动改用标准 1024x1024，文生图分辨率不受影响。
- 将 multipart 日志从“重试”改为“调用”，并补充编辑尺寸限制的 Dashboard 提示与公开排障说明。

## v3.2.1
- 修复 New API/OpenAI Images 的人设拍照、头像图生图和参考图请求：将误填为完整地址的裸 `/v1/images` 自动规范为 `/v1/images/generations` 或 `/v1/images/edits`。
- `gpt-image-*` 带参考图时直接使用标准 `multipart/form-data` 上传，避免 JSON/Base64 请求被上游收敛为不透明的 400 `openai_error`。
- Dashboard 新增每渠道“图生图上传格式”选择，并明确 New API/OpenAI Images 应填写站点基础地址而非裸 `/v1/images`。

## v3.2.0
- 修复 Dashboard 上传后的人设图、预设参考图预览：不再由 `<img>` 直连需要 Dashboard 授权的插件 API，而是通过已鉴权的页面配置响应返回受尺寸限制的内联缩略图。
- 为图片预览增加不可用状态，损坏的历史文件不会只显示浏览器破图标；删除按钮仍保留可用。
- Dashboard 改为默认夜间的赛博霓虹运维界面，以青色、洋红和琥珀色区分交互与状态；侧栏提供可持久化的浅色/夜间主题切换。
- 渠道保存继续写入 AstrBot `template_list` 所需的 `__template_key`，插件启动时会自动迁移旧数据。

## v3.1.0
- 恢复上游完整的 44 个默认提示词预设，并继续支持 Dashboard、聊天命令和预设参考图管理。
- 命令不再强制使用“灵绘”前缀：默认可直接使用 `#文生图`、`#画`、`#预设列表` 等短命令。
- 保留上游 `#bnn <提示词>` 自定义绘图兼容指令；即使将 `extra_prefix` 改为其他名称也可继续使用。
- 新增可选命令命名空间与“仅命名空间命令”开关；可配置为 `#绘图 文生图`，用于与其他绘图插件共存。
- Dashboard 增加命名空间与直接命令开关，并同步更新公开 README、元数据和内置帮助。

## v3.0.0
- 独立发布为 `astrbot_plugin_linghui_studio`（灵绘工坊），替换插件 ID、主类、LLM 工具、Web API 前缀和聊天命令，支持与上游插件共存。
- 重构访问控制：默认仅群白名单可生图；允许使用与无限次数名单分离，黑名单优先，管理员额度豁免可配置。
- 新增有序多渠道调度、失败自动回退和 `#通道 [ID/自动]` 快速切换。
- 新增 AstrBot Dashboard，用于管理渠道、模型、权限、额度、签到、提示词预设、提示词处理模型、人设和参考图。
- 新增可选提示词翻译/优化，以及 `#头像图生图`。
- 修复批量处理变量未初始化、并发数据写入不安全和动态配置保存不可靠等问题。

## v2.9.0
- 修复 `#切换模型` 在管理员 ID 类型不一致或旧模型列表格式异常时无响应的问题
- 统一 URL 自动补全：基础地址中的 v1/v1beta 和完整接口尾部会被忽略，再按所选模式生成唯一请求地址
- 新增 OpenAI Images、OpenAI Chat、Gemini 官方、自定义完整路径四种接口模式
- 配置面板移除旧 Generic/Gemini 独立地址、Key 池和 api_mode，仅保留统一接口配置；旧配置仍可静默读取
- `#切换API模式 generic` 等旧指令写法继续兼容，并映射到新的接口模式
- 自动识别提示词中的常规宽高比与 1K/2K/4K，并填充 OpenAI `size` 或 Gemini `imageConfig`
- 文生图默认比例可配置为 4:3，图生图会自动识别原图比例
- 补充 aiohttp、Pillow、PyMuPDF 插件依赖声明

## v2.8.8
- 优化上下文逻辑修改配置文件写入逻辑
